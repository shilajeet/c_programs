#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{char arr[100];
int i,n;
printf("\nEnter a number string=");
scanf("%s",arr);
n=strlen(arr);
for(i=0;i<n/2;i++)
{if(arr[i]==arr[n-i-1])
   {if(i==n/2-1)
   printf("Entered string is a palindrome");
   else
   continue;}
else
{printf("Entered string is not a palindrome");
break;}}
getch();}